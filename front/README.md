# front

Vite + React + MapLibre GL (react-map-gl, tiles do OpenFreeMap).

```
cd front && npm install && npm run dev     # proxy de /api para o back em :8080
npm run build                              # gera front/dist, servido pelo back no Render
```

Detalhes na Task 9 de `docs/PLAN.md`.
